.build/
vendor/node_modules/
vendor/package-lock.json
dist/
*.xcuserdatad
.DS_Store

# stray captures from development
*.png
# ...except the ones the README is built out of. Without this line the README
# ships with five broken images and looks fine locally, where the files exist.
!docs/images/*.png
